<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@drawable/rounded_gray"
    android:padding="10sp"
    android:layout_marginBottom="1dp"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <TextView
        android:id="@+id/activityItemName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Activity-1"
        android:textSize="18dp"
        android:letterSpacing="0.05"
        android:textColor="@color/white"
        android:textStyle="bold"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"/>

    <ImageButton
        android:id="@+id/activityItemUpdateBtn"
        android:layout_width="30dp"
        android:layout_height="40dp"
        android:src="@drawable/ic_pen"
        android:drawableTint="#FBC02D"
        android:clickable="true"
        android:backgroundTint="@color/gray"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="@id/activityItemName"
        app:layout_constraintRight_toLeftOf="@+id/activityItemDeleteBtn"/>

    <ImageButton
        android:id="@+id/activityItemDeleteBtn"
        android:layout_width="30dp"
        android:layout_height="40dp"
        android:src="@drawable/ic_delete"
        android:drawableTint="#D32F2F"
        android:clickable="true"
        android:backgroundTint="@color/gray"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="@id/activityItemName"
        app:layout_constraintRight_toRightOf="parent"/>

    <TextView
        android:id="@+id/activityItemDate"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="5sp"
        android:text="Monthly on 06/01 (MM/DD)"
        android:textSize="14dp"
        android:textColor="@color/white"
        app:layout_constraintTop_toBottomOf="@id/activityItemName"
        app:layout_constraintLeft_toLeftOf="parent"/>

    <TextView
        android:id="@+id/activityItemAmount"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="0/-"
        android:textSize="16dp"
        android:textColor="@color/green"
        android:textStyle="bold"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="@id/activityItemDate"/>

</androidx.constraintlayout.widget.ConstraintLayout>